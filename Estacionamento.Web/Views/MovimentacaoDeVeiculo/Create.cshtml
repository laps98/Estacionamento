@using Estacionamento.Domain.Enums;
@using Estacionamento.Domain.MovimentacoesDeVeiculo;
@model MovimentacaoDeVeiculo
@{
    ViewBag.Title = "Movimentação de veículo";
    var fluxos = Html.GetEnumSelectList<Fluxo>();

    // if (Model !=null)
    // {
    //     var fluxo = fluxos.FirstOrDefault(q => q.Value == ((int)Model.Fluxo).ToString());
    //     if (fluxo != null)
    //         fluxo.Selected = true;
    // }
}
@section scripts{
    <script src=""></script>
}
<form asp-controller="Cliente" asp-action="Create" method="post">
    <input type="hidden" asp-for="Id">
    <div>
        <h3>@ViewBag.Title</h3>
        <div class="card">
            <div class="card-body">
                <div class="row">
                    <div class="col-lg-6">
                        <div class="form-group">
                            <label for="Nome">Nome</label>
                            <input type="text" asp-for="Descricao" class="form-control" placeholder="Descricao">
                            @Html.ValidationMessageFor(q => q.Descricao)
                        </div>
                    </div>
                    <div class="col-lg-6">
                        <div class="form-group">
                            <label for="Fluxo">Fluxo</label>  

                            <select asp-for="Fluxo" asp-items="fluxos" class="form-control"></select>

@*                             @Html.DropDownList(
                            "StudentGender",
                            fluxos,
                            "Select Gender",new { @class = "form-control", aspfor="Fluxo" }) *@
                        </div>
                    </div>
                </div>

                <div>
                    <label for="Valor">Valor</label>
                    <input type="number" asp-for="Valor" class="form-control" id="Valor" />
                </div>

                <div class="mt-3">
                    <button class="btn btn-primary btn-block">Adicionar</button>
                </div>

            </div>
        </div>
    </div>
</form>
